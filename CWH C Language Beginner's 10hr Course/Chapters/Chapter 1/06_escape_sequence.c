#include<stdio.h>

int main() {
    printf("This creates a \t tab. This creates a new \n line.\n");
    printf("I am using \" inside printf and also \\ inside printf\n");
}

//You can actually skip the return 0; in modern compilers but it is generally not recommened
// Although it is ok to skip for small program
// This works for c99 with gcc compiler