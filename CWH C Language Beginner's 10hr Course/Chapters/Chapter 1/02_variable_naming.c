#include <stdio.h>

int main()
{
    int a;
    int _hi;
    int fun_car;
    // int 7;   This is not allowed bcz variable name cant be number
    // int @**^^#)!@    Special symbols are not allowed except underscore _
    return 0;
}

// Unlike Python, in C we have to first declare the variable, then intialize the variable
// And then we can print the value